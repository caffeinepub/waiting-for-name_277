/**
 * Badge.tsx — custom variants extending shadcn badge
 * Import from here throughout the app (not from @/components/ui/badge)
 */
export { Badge, badgeVariants } from "./badge";

// Additional custom badge variant CSS is applied via index.css (.badge-emergency)
